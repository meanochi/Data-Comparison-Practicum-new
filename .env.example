# Server Configuration
PORT=3000
NODE_ENV=development

# Upload limits
UPLOAD_MAX_FILE_SIZE_MB=50
JSON_BODY_LIMIT_MB=200

# לתחקור בלבד: כשמוגדר, כל בקשת POST /api/compare נשמרת שם כקובץ JSON
# API_DUMP_DIR=
